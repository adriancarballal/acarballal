<html xmlns:t="http://tapestry.apache.org/schema/tapestry_5_0_0.xsd"
	t:type="Layout" t:pageTitle="${message:title}">

<p>${message:aboutMe}.</p>
   
   <t:block t:id="noCommentBlock">
	
	<t:if test="commentGridDataSource.commentsNotFound">
		<p>${message:commentsNotFound}.</p>
		<t:parameter name="else">
			<p>${message:nocomments}.</p>
		</t:parameter>
	</t:if>
    
</t:block>
	
<!--
+ It is necessary to explicitly add "date" property since "Grid" component does
  not recognize "Calendar" properties.
-->
<t:grid source="commentGridDataSource" row="userComment" 
		reorder="comment, date" remove="commentId"
		rowsPerPage="rowsPerPage" add="date">

	<t:parameter name="dateCell">
		<t:output value="userComment.date.time" format="dateFormat"/>
	</t:parameter>
		
</t:grid>

<br/>
   
</html>