Create a database with name "j2ee".
	- mysqladmin -u root create elmas     
Create a user with name "elmas" and password "elmas", and allow him to access from local host.
    - mysql -u root
    - GRANT ALL PRIVILEGES ON elmas.* to elmas@localhost IDENTIFIED BY 'elmas',
         elmas@localhost.localdomain IDENTIFIED BY 'elmas';
Try to access to "elmas" database as "elmas" user with password "elmas".
	- mysql -u elmas --password=elmas elmas
Shutdown database
	- mysqladmin -u root shutdown
	
	--> USO DE BACHSIZE!!!!!!!!
	- mirar votos de un video - preguntar	
	- usuarios no registrados pueden ver los comentarios? eleccion

	- show usercomments boton siguiente no funciona bien? compr	obar videocomments
	- cambiar el texto eliminar por el icono
	- usuarios no registrados pueden ver los comentarios?
	- cuando eliminamos un video eliminamos los archivos?
	- si eliminamos todos en una busqueda de una pagina no vuelve a la pagina anterior
	  si esta existe
	- ThreadGroup
	- Utilizar en encoderService
	
	faltan los junit para los metodos:
	public VideoBlock findVideosByUser(Long userId, int startIndex, int count);
	public List<VideoComment> findVideoCommentsByUser(Long userId);
	Para los complaints
	Para vote
	para favoritos
	para mensajes