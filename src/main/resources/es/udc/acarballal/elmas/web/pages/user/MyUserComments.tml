<html xmlns:t="http://tapestry.apache.org/schema/tapestry_5_0_0.xsd"
	t:type="Layout" t:pageTitle="${message:title}">

<t:if t:test="userComments.empty">

	<p>${message:nocomments}.</p>
	
<t:parameter name="else">

	<p>${message:myComments}.</p>

	<table class="comments">
		<tr t:type="Loop" t:source="userComments" t:value="userComment">
			<tr>
				<td class="user-comment">${userComment.comment}</td>
			</tr>
			<tr><td class="commentator-info">
				${message:user-header}: ${userComment.commentator.loginName}
			 - 
				${message:date}: <t:output value="userComment.date.time" format="dateFormat"/>
			 - 
			 <a href="#" t:type="PageLink" page="user/DeleteUserComment"
	    		context="userComment.commentId">
	    		${message:delete}
	    	</a>
				</td>
			</tr>
		</tr>
	</table>
	
		
	<!-- "Previous" and "Next" links. -->
		
	<div class="previousNextLinks">
	<span class="previousLink">
		<t:if t:test="previousLinkContext">
			<a href="#" t:type="PageLink" page="user/MyUserComments"
	    		context="previousLinkContext">
	    		${message:link-previous}
	    	</a>
		</t:if>
	</span>
	
	<span class="nextLink">
		<t:if t:test="nextLinkContext">
			<a href="#" t:type="PageLink" page="user/MyUserComments"
	    		context="nextLinkContext">
	    		${message:link-next}
	    	</a>
		</t:if>
	</span>
	</div>
	
</t:parameter>
</t:if>

   
</html>